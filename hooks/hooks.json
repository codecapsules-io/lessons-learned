{
  "description": "Blocks Write/Edit/Bash calls that would put a secret, credential, or oversized code block into a lessons-learned file. Denies (rather than silently allowing) if Node.js isn't available to run the scanner, but only for calls that look lessons-learned-related — unrelated work is unaffected either way.",
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Write|Edit|MultiEdit|Bash",
        "hooks": [
          {
            "type": "command",
            "command": "sh \"${CLAUDE_PLUGIN_ROOT}/hooks/scan-write-wrapper.sh\"",
            "timeout": 10
          }
        ]
      }
    ]
  }
}
